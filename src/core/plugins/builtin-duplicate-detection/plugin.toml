[plugin]
id = "builtin-duplicate-detection"
name = "dHash Duplicate Detection Provider"
version = "1.0.0"
capability = "duplicate"
entry_point = "inproc"
runtime = "python"
permissions = ["read:image_bytes"]
model_source = "bundled"

[compatibility]
core_api_version = ">=1.0,<2.0"
